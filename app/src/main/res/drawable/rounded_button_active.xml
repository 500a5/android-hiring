<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blue" /> <!-- Голубой фон -->
    <corners android:radius="8dp"/>
    <stroke android:color="@color/blue_dark" android:width="2dp"/> <!-- Синяя обводка -->
</shape>